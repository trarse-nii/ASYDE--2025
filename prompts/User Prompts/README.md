# User Prompts

This directory contains the 2 prompt templates used to provide the initial model and ask for a repair. The `user_prompt_with_previous_specification.md` is used when a given model is a refined version of a previous one, and `user_prompt.md` is used otherwise.